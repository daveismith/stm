import React from "react";
import styled from 'styled-components';

const View: React.FC = () => {
      
    const StyledRoot = styled.div`
        color: #ffffff;
        margin: 24px auto;
    `;

    return (  
        <StyledRoot>
            VIEW
        </StyledRoot>
    );
};

export default View;
